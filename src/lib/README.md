lib/components/ui - components from the Coss UI suite
lib/components/shared + lib/components/app - will eventually merge into lib/components/app, those are the components specific to the App
lib/fonts - the fonts that get embedded
lib/formsnap - a modified version of formsnap that allows the positioning of Labels outside a Control
lib/varsel - a toast implementation, to check if I need to bundle it
lib/registry - holds the particules/component metadata for the app. it remders things like title, descriptions..etc
lib/assets - static assets like fonts and images